import React, { useState } from 'react';
import { useParams } from 'react-router-dom';
import axios from 'axios';

// this is used for navigation and the routing, so all the links work properly
const Post = () => {
   return (
      <div className="container my-4">
         <h1 className="mb-4">Placdholder Title</h1>
      </div>
   )
};

export default Post;